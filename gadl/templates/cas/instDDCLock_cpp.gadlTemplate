%template headerC%
%if useCAS then
%#include "instruction.h" //first for precompiled header
#include <iostream>
#include "arch.h"

using namespace std;

// instructions that lock registers for the data dependency controller.

%foreach inst in instDDCLock do
  %void %!inst::name%::lockDDCWriteRegs(%!paramDDCLock%){
%!inst::lock
%}
%
between %\n%
end foreach
end if #useCAS
