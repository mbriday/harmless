%template headerC
if useCAS then%
#include "instruction.h" //first for precompiled header
#include <iostream>
#include "arch.h"

using namespace std;

// instructions that require registers for the data dependency controller.

%foreach inst in instDDCRequired do
  %void %!inst::name%::getDDCReadRegs(%!paramDDCRequire%){
%!inst::required
%}
%
between %\n%
end foreach
end if #useCAS
