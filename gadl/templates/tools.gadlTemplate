%# This file is used to generate 'tools' used by gadl.
#This template is NOT called by the 'root' template, because
#some file should be used by gadl itself (the root templates is
#only executed at the end of gadl computation).

#required for timing tools (inside a peripheral).
template timingTools in "peripheral/timing"
%
